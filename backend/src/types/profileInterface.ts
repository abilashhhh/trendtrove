export interface ProfileInterface {
    userId?: string,
    name?: string,
    username?: string,
    email?: string,
    phoneNumber?: number,
    bio?: string,
    gender?: string,
    address?: string,
    savedPosts?: string[],
}